Produce a SLIDE OUTLINE for a Beamer presentation, built ONLY from the
transcript and description above. This is NOT prose — it is the skeleton of a
talk.

Rules:
- Start with a single top-level title line (the talk title).
- Then a sequence of FRAMES. Each frame is a short Title-Case heading line (a
  `##` markdown heading), followed by 2 to 6 terse bullet points (`-` bullets).
- Each bullet is a phrase, not a sentence: no more than ~12 words, no trailing
  period, no paragraphs.
- Keep the key technical terms, definitions, named results, and equations
  exactly as stated in the transcript (write equations inline, e.g. `Δ = I -
  D^{-1/2} A D^{-1/2}`).
- Follow the argument order of the video: one frame per distinct idea/step.
- If the transcript names papers, people, or sources, add a final `## References`
  frame listing them as `[n]` numbered bullets — verbatim, invent nothing.
- Do NOT include a BibTeX block, speaker notes, prose paragraphs, or commentary.

Aim for roughly one frame per major point (typically 8–20 frames for a talk).
